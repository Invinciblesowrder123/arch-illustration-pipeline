# 考古学论文插图生成助手 — 使用说明

本工具帮您完成一条完整的学术论文插图生成流程：

```
① 描述绘图需求 → ② 放入参考文献 → ③ AI 学习文献知识（必要时联网补充）
→ ④ AI 绘图 → ⑤ AI 审稿校验 → ⑥ 有问题自动重绘，通过则输出最终插图
```

## 一、首次安装（只需做一次）

1. 安装 Python 3.10+（安装时勾选 "Add Python to PATH"）。
2. 在本目录打开命令行（资源管理器地址栏输入 `cmd` 回车），执行：

```
pip install -r requirements.txt
```

3. 直接运行 `python main.py`，首次启动会引导您输入**两个 API Key**（均为 aixw 的密钥）：
   - **LLM Key**：语言模型 gpt-5.6-sol 所在分组的密钥（负责读文献、审稿校验）；
   - **绘图 Key**：绘图模型 gpt-image-2 所在分组的密钥（负责生成插图）。
   - 若两个模型在同一分组，两处填同一个即可；第二步直接回车可复用第一个。
   其余配置（服务商地址、模型名）均已预设，自动写入 `.env`。
4. 密钥填好后，脚本会**自动做一次连通性自检**：文本对话、视觉读图、绘图三项各实测一次，
   全部 ✅ 才继续。以后随时可用 `python main.py --check` 重新自检（`--skip-image` 可跳过绘图测试省钱）。

> 若需换服务商或模型，用记事本打开 `.env` 修改对应行即可，无需改代码。

## 二、每次绘图的三个步骤

**第 1 步：放参考文献。** 把绘图所依据的文献（PDF / Word / TXT / Markdown）
全部放进项目里的 `references` 文件夹。

**扫描版 PDF 也能读**：程序会自动按"MinerU 本地解析（免费）→ 视觉直读（把页面
渲染成图片交给多模态模型阅读）→ 跳过"的顺序处理，遇到扫描版时会询问您选哪种。
如果本机装了 MinerU 会自动检测到；没有装也可以选视觉直读（会多消耗一些 token）。

**第 2 步：描述绘图需求。** 两种方式任选：
- 把需求写进项目根目录的 `requirement.txt` 文件（推荐，方便反复修改）；
- 或运行命令后按提示输入。

需求写得越具体效果越好，建议包含：**绘制对象、时代/文化、视图角度、
风格（线描图/复原图/剖面图）、必须出现的要素、需要标注的文字**。

示例：
```
绘制二里头文化绿松石龙形器的科学复原图：俯视 45 度角，
以白描线图风格呈现，需表现 2000 余片绿松石的镶嵌结构，
龙身曲置呈"匚"形，在图右侧用引线标注吻部、菱形主纹、尾尖三处，
画面下方附 5 厘米比例尺。依据文献中的发掘报告描述，不得添加文献未记载的纹饰。
```

**第 3 步：运行。**

```
python main.py
```

然后等待。工具会自动：读文献 → 学知识 → 绘图 → 审稿校验 → 不合格自动重绘（默认最多 3 轮）。

## 三、看结果

全部产物在 `output` 文件夹下按运行时间归档：

| 文件 | 说明 |
|---|---|
| `final_illustration.png` | ✅ 最终插图（已通过校验） |
| `report.md` | 生成报告：每一轮的问题清单与修改建议 |
| `illustration_attempt_N.png` | 各轮历史版本 |
| `knowledge/knowledge_summary.md` | AI 从文献中汇总的绘图知识（可人工核对） |
| `knowledge/illustration_spec.json` | 结构化绘图规格 |

**重要**：`report.md` 里若有"⚠️ 达到最大重试次数仍未通过"，说明 AI 审稿
发现插图与文献存在出入，问题已逐条列出——请据此修改 `requirement.txt`
或补充文献后重跑。

## 三点五、图不满意时：指定重绘（v1.1.0 新增）

看图后觉得某几处要改（而不是整张重来），把意见直接写进命令：

```powershell
python main.py --revise output/run_20260922_183750 --feedback "图内标注改成中文；底部比例尺移到右下角；镶嵌片不要规则化"
```

- 产物在 `output/run_20260922_183750_rev1/`，报告会**逐条**回答：这条改了没有、证据在图面什么位置；
- **没提到的方面不会被顺手改动**（提示词里禁止 + 校验里会检查），所以不会"越改越跑偏"；
- 想继续改就再跑一次，把 `--revise` 指向上一次的 `_rev1` 目录即可（必须保持的项会累积继承）；
- 费用：每出一版图算一张图的费用（`--max-attempts` 控制最多重绘几轮）。

**图内文字**：默认 `caption_only`（图内不出现任何文字，名称放在**图下图注表**里，报告会给出
"编号｜名称｜图上位置"对应表）——这也是考古线图最常见的做法。若希望标注直接写在图上，加
`--text-mode in_image`（会强制简体中文）。建议两种各出一版给教授挑。

## 四、常用命令

| 命令 | 作用 |
|---|---|
| `python main.py` | 正式生成插图 |
| `python main.py --revise output/run_xxx --feedback "…"` | **按您的意见重绘**（报告逐条回应） |
| `python main.py --text-mode in_image` | 图内标注写中文（默认图内无文字 + 图注表） |
| `python main.py --check` | 模型连通性自检（文本/读图/绘图各实测一次） |
| `python main.py --check --skip-image` | 自检但跳过绘图测试（不产生绘图费用） |
| `python main.py --dry-run` | 只检查文献能否正常读取，不花钱 |
| `python main.py --max-attempts 5` | 增加重绘轮数 |
| `python main.py --no-search` | 禁止联网补充知识（只用您给的文献） |
| `python main.py -r "需求文字"` | 直接在命令里给需求 |

## 五、已知事项与常见问题

- **✅ 绘图模型可用性（已解决）**：aixw 的语言模型与绘图模型分属不同分组、需要不同的密钥。
  已实测：LLM Key 调 gpt-5.6-sol（含读图）、绘图 Key 调 gpt-image-2 均正常（2026-09-22）。
  若某天报 "not available for this group"，说明对应密钥的分组没开通该模型——
  去服务商控制台换分组/换 key，或在 `.env` 的 `IMG_BASE_URL / IMG_API_KEY / IMG_MODEL` 换其他绘图服务商。
- **提示"参考文献目录为空"**：文献还没放进 `references` 文件夹。
- **提示"文件内容过少（疑似扫描版 PDF）"**：扫描版 PDF 会自动尝试 MinerU 解析或视觉直读，全部失败时才会报错；此时可换文字版或检查 MinerU 安装。
- **提示"缺少必需环境变量"**：首次启动向导没跑完，删除 `.env` 后重新运行 `python main.py`。
- **想换绘图服务商**：只改 `.env` 里的地址、密钥、模型名即可，无需改代码。
