# 给教授的确认清单

> 项目：考古学论文插图生成流水线（arch-illustration-pipeline）
> 版本：v1.1.0（提交 39bb615）
> 需要您：提供 3 项资料 · 拍板 3 项选择 · 给出逐条图意见

---

## 目录

1. 速览：需要您这边的 6 件事
2. 需要您提供的资料
3. 需要您拍板的选择
4. 拉下项目后怎么跑
5. 看图提意见怎么写
6. 回执表

---

## 一、速览：需要您这边的 6 件事

| # | 事项 | 为什么需要您 | 您怎么回 | 卡住什么 |
|---|---|---|---|---|
| 1 | **20 组历史绘图需求 + 期望命中的文献/页码** | 这是检索质量的"标尺"：没有它，检索参数一动就没人知道变好还是变坏 | 表格或文字都行，格式见 §2.1 | 检索调参、全量入库后的验收 |
| 2 | **3 篇付费文献**：Ferrell 1969（纸本 444 页）、Grace 1961（*American Anthropologist* 63:359-368，Wiley）、Grace 1964（*Current Anthropology* 5:361-368，Chicago） | 无合法免费源；需机构订阅或纸本扫描 | 提供全文 PDF，或告知走什么渠道 | 这 3 篇进不了知识库 |
| 3 | **2 个坏文件重下**：`Pawley_Green_Chapter3.pdf`（13KB，截断）、`Pawley_Prehistory_Oceanic_Languages.pdf`（42KB，截断） | 原件下载中断，打不开 | 重新下载后交给我们 | 这 2 篇进不了知识库 |
| 4 | **确认 `Pawley_Green_Chapter3` 的原始目标** | 疑似即已入库的《The Austronesians》第 3 章（Pawley & __Ross__），若确认是同一篇就不必再找 | 是／不是（一句话） | 避免重复入库或漏一篇 |
| 5 | **RAGFlow 服务器的内存规格**（≥32GB？） | 嵌入模型 `bge-m3` 在 CPU 上实测独占 **17.1GB**，整栈约 20.5GB——**16GB 机器跑不动**；且换嵌入模型要重算全库向量，必须入库前定 | 告知服务器内存／是否可用外部嵌入 API | **阻塞全部 215 篇入库** |
| 6 | **图内标注方式二选一**（见 §3.1） | 这是您上次反馈"图里是英文"的正解方向，两种都跑得通，由您定 | 勾选 A / B，或先看样图 | 批量出图的默认配置 |

> 另外还有一件不是"提供资料"、而是"给意见"的事：对已生成的样图**逐条**提修改意见（§5）。
> 这一版专门做了入口，您提的意见会被**逐条落实、逐条回执**。

---

## 二、需要您提供的资料

### 2.1 二十组历史绘图需求（最重要的一项）

请回想或翻找 20 个**真实提过或典型**的绘图需求，并为每组写出"期望命中的文献和页码"。
格式随意，能对上就行：

| # | 绘图需求（一句话） | 期望命中的文献 | 期望页码（可留空） |
|---|---|---|---|
| 1 | 绘制二里头文化绿松石龙形器的科学复原图（俯视 45°，白描线图） | 《二里头 1999—2006》发掘报告 | p.XXX |
| 2 |  |  |  |
| 3 |  |  |  |

**为什么用这个格式**：系统会拿这 20 组去自动跑检索，统计命中率与前几名命中明细。
以后凡是动检索、切片、阈值，都先跑它——**它是防止我们"越调越差"的唯一护栏**。
页码填不出来就留空，只填文献名也能用。

### 2.2 三篇付费文献（无合法免费源）

| 文献 | 出处 | 需要什么 |
|---|---|---|
| Ferrell 1969 | 纸本，444 页 | 纸本扫描，或采购 |
| Grace 1961 | *American Anthropologist* 63:359-368（Wiley 付费墙） | 机构订阅下载 |
| Grace 1964 | *Current Anthropology* 5:361-368（Chicago 付费墙） | 机构订阅下载 |

### 2.3 两个坏文件

| 文件 | 症状 |
|---|---|
| `Pawley_Green_Chapter3.pdf` | 13KB，下载截断，无法解析 |
| `Pawley_Prehistory_Oceanic_Languages.pdf` | 42KB，下载截断，无法解析 |

请重新下载，或从原库重新导出。

---

## 三、需要您拍板的选择

### 3.1 图内标注：两种方式二选一

| | **A. caption_only（当前默认）** | **B. in_image** |
|---|---|---|
| 图内长什么样 | **图内不出现任何文字**，只用细引线指出标注位置 | 标注文字**写在图上**，强制简体中文 |
| 名称放哪 | **图下图注表**：编号 ｜ 名称 ｜ 图上位置（报告里自动生成，正文可直接用） | 图内 |
| 学术惯例 | 考古线图最常见的做法（图内标号、图版说明给名称） | 图版中也常见，但依赖模型把中文写对 |
| 风险 | 需正文配图注表 | 文生图模型渲染中文**不稳定**：可能出现伪汉字、缺笔，校验会把这类判为不通过并要求重画 |
| 校验规则 | 图内出现任何文字、字母、数字即不通过 | 出现英文、乱码、不可辨即不通过 |

**建议**：由我们各出一版样图（同一需求、同一知识），您对比后勾一个。
若选 A 又想在图面上看到编号数字，请一并说明——**默认 A 是连数字也不出现的**
（图注表用"图上位置"这一列来描述引线指向哪儿）。

### 3.2 服务器与嵌入模型（阻塞 215 篇入库）

现状实测：嵌入模型选 `BAAI/bge-m3`（CPU）时独占 **17.1GB** 内存，整栈约 20.5GB。
三条路，需您告知哪条可行：

| 选项 | 代价 |
|---|---|
| ① 服务器内存 ≥32GB | 最省事，吞吐最高 |
| ② 换 `Qwen/Qwen3-Embedding-0.6B`（约 5GB） | **已入库的向量要作废重算**（所以必须入库前定）；检索质量需重新评估 |
| ③ 嵌入走外部 API | 有网络与费用依赖 |

### 3.3 入库排期

215 篇真实语料的全量入库占机器时间较长（实测：论文 3–5 页/分钟，扫描专著约 2.3 页/分钟）。
需要您告知：**希望先入哪一部分**（例如先入论文组 165 篇，再入专著 14 本 / 5108 页）。

---

## 四、拉下项目后怎么跑

### 4.1 拉取（PowerShell）

```powershell
git clone https://github.com/Invinciblesowrder123/arch-illustration-pipeline.git
cd arch-illustration-pipeline
pip install -r requirements.txt
```

- 默认拿到的就是最新主线（含本清单）。已克隆过则 `git pull` 即可。
- 右边栏 **Releases** 页有 v1.1.0 的发布说明（这一版改了什么、怎么用）。
- 若本机没装 git：在仓库页面点 `Code` → `Download ZIP`，解压同样可用。

### 4.2 填密钥

密钥由我们这边单独提供（仓库里只有 `.env.example` 占位，不含任何真实密钥）。
把 `.env.example` 复制为 `.env`，填三处：语言模型 Key、绘图 Key、视觉校验 Key（默认复用语言模型）。

### 4.3 三条命令

```powershell
python main.py --check --skip-image
python main.py --mode rag
python main.py --revise output/run_xxxx --feedback "您的意见"
```

第一条是自检（连通性 + 环境指纹，零绘图费用）；第二条正式出图（默认最多 3 轮自动重绘）；
第三条是看图之后按意见重绘（见 §5）。

### 4.4 产物在哪、看什么

| 路径 | 看什么 |
|---|---|
| `output/run_时间戳/report.md` | **主报告**：知识来源与检索词、引用文献（含页码）、图注表、每轮问题清单与评分 |
| `output/run_时间戳/illustration_attempt_N.png` | 各轮尝试图（最后一张不一定通过） |
| `output/run_时间戳/final_illustration.png` | 通过校验的成品（没有这个文件 = 没通过） |
| `knowledge/knowledge_summary.md` | AI 汇总的绘图知识——**请重点核对**：条目是否忠实于文献、页码是否对得上 |

---

## 五、看图提意见怎么写

这一版的意义就在这儿：**您不必改需求从头再来**。写一段话即可，系统会
①拆成若干条 → ②逐条改 → ③**逐条回执**（改了没有 + 证据在图面哪个位置）。

写法就三件事：

1. **哪一处不对**——尽量指到图面位置（"画面下方居中"比"整体"有用）
2. **应该是什么**——要具体、可判定
3. **哪些地方不许动**——没说的我们也会默认不动，但您明说更稳

**好的示例**

```text
1. 图内标注不要英文，改成中文，写在图外图注表里也行；
2. 底部那根比例尺移到右下角，不要压在器物下方；
3. 镶嵌片的边界不要画得太规则，现在像砖墙；
4. 构图、视角、器物形制不要改。
```

**无效的示例**（无法判定，系统只能整段照抄，效果差）

```text
整体还不够准确，再优化一下，风格再学术一点。
```

一次 2–6 条最佳，每条一行。

---

## 六、回执表

### 6.1 资料类

- [ ] 二十组检索回归集 —— 附件：__________
- [ ] 三篇付费文献 —— 附件：__________
- [ ] 两个坏文件重下 —— 附件：__________
- [ ] `Pawley_Green_Chapter3` 是否即《The Austronesians》第 3 章：☐ 是　☐ 不是　☐ 不确定

### 6.2 选择类

- [ ] 图内标注：☐ A caption_only　☐ B in_image　☐ 先看样图再定
- [ ] 嵌入方案：☐ ① ≥32GB 服务器　☐ ② 换 0.6B 模型　☐ ③ 外部 API
- [ ] 入库顺序：☐ 先论文组　☐ 先专著组　☐ 其他：__________
- 服务器信息（内存／核数）：__________

### 6.3 意见类

- [ ] 已对样图给出逐条意见（见附页或邮件）

### 6.4 其他想说的

```text
（还需要哪些器物、哪些图版；对风格的要求；对报告格式的要求等）
```

---

*本清单随 v1.1.0 发布。技术细节见仓库内 `README.md` 与 `CHANGELOG.md`；
开发进展与已知坑见 `docs/DEV_TASKS.md` 与 `docs/HANDOFF.md`。*
